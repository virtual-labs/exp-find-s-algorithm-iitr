#### Please attempt the following questions

<br>
Q1. In the Find-S algorithm, what happens if an instance contradicts the current most specific hypothesis?<br>
a. The instance is discarded from the training set. <br>
b. The instance is added to the testing set. <br>
c. The most specific hypothesis is updated to exclude the instance. <br>
d. The most specific hypothesis is updated to include the instance. <br>
<br>

Q2. The Find-S algorithm is designed to work with:<br>
a. Labeled data only <br>
b. Unlabeled data only <br>
c. Labeled and unlabeled data <br>
d. Noisy data only <br>
<br>

Q3. Find-S algorithm is a:<br>
a. Model-based learning algorithm <br>
b. Instance-based learning algorithm <br>
c. Rule-based learning algorithm <br>
d. Lazy learning algorithm <br>
<br>

Q4. The Find-S algorithm terminates when:<br>
a. A specific number of iterations is reached <br>
b. The training data is exhausted <br>
c. The most specific hypothesis becomes empty <br>
d. The most general hypothesis becomes empty <br>
<br>

Q5. Which of the following is a limitation of the Find-S algorithm?<br>
a. It cannot handle noisy data <br>
b. It requires a large number of training instances <br>
c. It only works with continuous data <br>
d. It is not suitable for binary classification tasks <br>
<br>

